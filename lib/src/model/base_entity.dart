import 'dart:convert';

class BaseEntity {
  /*T fromMap<T>(Map<String, dynamic> json) => js<T>(json);

  Map<String, dynamic> toMap() => jsonDecode(this.toString());

  @override
  String toString() => jsonEncode(this);*/
}
